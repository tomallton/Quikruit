def generate_score():
  if (there are enough labeled applications on the database):
    return supervised()
  else:
    return unsupervised()
    
def supervised():
  # a logistic regression approach
  
def unsupervised():
  
